from collections import Counter
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:

        result= [a for a,b in Counter(nums).most_common(k)]
        return result
        
    
        
    
